 LLM Bug Detector
A command-line tool that uses a Large Language Model (Claude) to automatically detect bugs, logic errors, and code quality issues in Python source files.
Features

Detects bugs with severity levels (critical / warning / info)
Shows line numbers and concrete fix suggestions
Outputs a code quality score (0–10)
Supports raw JSON output for pipeline integration
Zero external dependencies — uses only Python stdlib